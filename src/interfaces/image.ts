export interface Image {
  id: string;
  cropped_picture: string;
}

export interface ImageDetails {
  author: string;
  camera: string;
  cropped_picture: string;
  full_picture: string;
  id: string;
  tags: string;
}
