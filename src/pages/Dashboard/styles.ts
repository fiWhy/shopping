import { Gallery } from '../../components';
import { styled } from '../../styles';
import { Grid } from '../../styles/grid';

export const DashboardWrapperStyled = styled(Grid)`
  height: 100%;
  width: 100%;
`;

export const GalleryStyled = styled(Gallery)`
  margin-top: 40px;
`;
