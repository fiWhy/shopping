import { styled } from '../../../styles';
import { Grid } from '../../../styles/grid';

export const SearchWrapperStyled = styled(Grid)``;

export const SearchStyled = styled.input`
  width: 100%;
`;
